#include <stdio.h>

int main() {
    int n, a, b;

    if (scanf("%d", &n) != EOF) {
        for (int i = 0; i < n; i++) {
            if (scanf("%d %d", &a, &b) != EOF) {
                printf("%d\n", a + b);
            }
        }
    }
    return 0;
}

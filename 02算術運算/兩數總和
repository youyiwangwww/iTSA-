#include <iostream>
using namespace std;

int main() {
    int n, a, b;

    if (cin >> n) {
        for (int i = 0; i < n; i++) {
            if (cin >> a >> b) {
                cout << a + b << '\n';
            }
        }
    }

    return 0;
}

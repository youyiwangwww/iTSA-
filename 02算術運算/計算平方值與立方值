#include <iostream>
using namespace std;

int main() {
    int n;
    int M;

    cin >> n;

    for (int i = 0; i < n; i++) {
        cin >> M;

        int square = M * M;
        int cube = M * M * M;

        cout << M << " " << square << " " << cube << '\n';
    }

    return 0;
}

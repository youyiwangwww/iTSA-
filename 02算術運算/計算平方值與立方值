#include <stdio.h>

int main() {
    int n;
    int M;

    scanf("%d", &n);

    for (int i = 0; i < n; i++) {
        scanf("%d", &M);
        int square = M * M;
        int cube = M * M * M;
        printf("%d %d %d\n", M, square, cube);
    }

    return 0;
}

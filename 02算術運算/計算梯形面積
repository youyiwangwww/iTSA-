#include <stdio.h>

int main() {
    int T, B, H;
    double area;

    while (scanf("%d %d %d", &T, &B, &H) != EOF) {

        //除以 2.0 而非 2
        area = (double)(T + B) * H / 2.0;
        printf("Trapezoid area:%.1f\n", area);
    }

    return 0;
}

#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    int maxValue;

    cin >> a >> b >> c;

    maxValue = a;

    if (b > maxValue) {
        maxValue = b;
    }

    if (c > maxValue) {
        maxValue = c;
    }

    cout << maxValue << '\n';

    return 0;
}

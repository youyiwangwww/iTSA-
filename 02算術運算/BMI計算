#include <stdio.h>

int main() {
    int w, h;
    double height, bmi;

    scanf("%d %d", &w, &h);

    height = h / 100.0;

    bmi = w / (height * height);

    printf("%.2f\n", bmi);

    return 0;
}

#include <iostream>
using namespace std;

int main() {
    int M, N, r;

    cin >> M >> N;

    // 輾轉相除法求最大公因數
    while (N != 0) {
        r = M % N;
        M = N;
        N = r;
    }

    cout << M << '\n';

    return 0;
}

#include <iostream>
using namespace std;

int main() {
    int n;
    int x, y;

    cin >> n;

    for (int i = 0; i < n; i++) {
        cin >> x >> y;

        int sum = x + y;

        cout << sum * sum << '\n';
    }

    return 0;
}

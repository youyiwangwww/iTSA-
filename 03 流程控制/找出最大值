#include <iostream>
using namespace std;

int main() {
    int n, x, maxValue;

    cin >> n;

    cin >> maxValue;

    for (int i = 1; i < n; i++) {
        cin >> x;

        if (x > maxValue) {
            maxValue = x;
        }
    }

    cout << maxValue << '\n';

    return 0;
}
